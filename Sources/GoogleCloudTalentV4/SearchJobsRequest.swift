// Code generated by sidekick. DO NOT EDIT.
//
// Copyright 2026 Google LLC
//
// Licensed under the Apache License, Version 2.0 (the "License");
// you may not use this file except in compliance with the License.
// You may obtain a copy of the License at
//
//     https://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing, software
// distributed under the License is distributed on an "AS IS" BASIS,
// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
// See the License for the specific language governing permissions and
// limitations under the License.

import Foundation
@_spi(GoogleCloudInternal) import GoogleCloudWkt

/// The Request body of the `SearchJobs` call.
public struct SearchJobsRequest: Codable, Equatable, GoogleCloudWkt._AnyPackable,
  Sendable
{
  /// Required. The resource name of the tenant to search within.
  ///
  /// The format is "projects/{project_id}/tenants/{tenant_id}". For example,
  /// "projects/foo/tenants/bar".
  public var parent: Swift.String = Swift.String()

  /// Mode of a search.
  ///
  /// Defaults to
  /// [SearchMode.JOB_SEARCH][google.cloud.talent.v4.SearchJobsRequest.SearchMode.JOB_SEARCH].
  ///
  /// [google.cloud.talent.v4.SearchJobsRequest.SearchMode.JOB_SEARCH]: <doc:SearchJobsRequest/SearchMode/jobSearch>
  public var searchMode: SearchJobsRequest.SearchMode = SearchJobsRequest.SearchMode()

  /// Required. The meta information collected about the job searcher, used to
  /// improve the search quality of the service. The identifiers (such as
  /// `user_id`) are provided by users, and must be unique and consistent.
  public var requestMetadata: RequestMetadata? = nil

  /// Query used to search against jobs, such as keyword, location filters, etc.
  public var jobQuery: JobQuery? = nil

  /// Controls whether to broaden the search when it produces sparse results.
  /// Broadened queries append results to the end of the matching results
  /// list.
  ///
  /// Defaults to false.
  public var enableBroadening: Swift.Bool = Swift.Bool()

  /// An expression specifies a histogram request against matching jobs.
  ///
  /// Expression syntax is an aggregation function call with histogram facets and
  /// other options.
  ///
  /// Available aggregation function calls are:
  /// * `count(string_histogram_facet)`: Count the number of matching entities,
  /// for each distinct attribute value.
  /// * `count(numeric_histogram_facet, list of buckets)`: Count the number of
  /// matching entities within each bucket.
  ///
  /// A maximum of 200 histogram buckets are supported.
  ///
  /// Data types:
  ///
  /// * Histogram facet: facet names with format `[a-zA-Z][a-zA-Z0-9_]+`.
  /// * String: string like "any string with backslash escape for quote(\")."
  /// * Number: whole number and floating point number like 10, -1 and -0.01.
  /// * List: list of elements with comma(,) separator surrounded by square
  /// brackets, for example, [1, 2, 3] and ["one", "two", "three"].
  ///
  /// Built-in constants:
  ///
  /// * MIN (minimum number similar to java Double.MIN_VALUE)
  /// * MAX (maximum number similar to java Double.MAX_VALUE)
  ///
  /// Built-in functions:
  ///
  /// * bucket(start, end[, label]): bucket built-in function creates a bucket
  /// with range of [start, end). Note that the end is exclusive, for example,
  /// bucket(1, MAX, "positive number") or bucket(1, 10).
  ///
  /// Job histogram facets:
  ///
  /// * company_display_name: histogram by
  /// [Job.company_display_name][google.cloud.talent.v4.Job.company_display_name].
  /// * employment_type: histogram by
  /// [Job.employment_types][google.cloud.talent.v4.Job.employment_types], for
  /// example,
  ///   "FULL_TIME", "PART_TIME".
  /// * company_size (DEPRECATED): histogram by
  /// [CompanySize][google.cloud.talent.v4.CompanySize], for example, "SMALL",
  /// "MEDIUM", "BIG".
  /// * publish_time_in_day: histogram by the
  /// [Job.posting_publish_time][google.cloud.talent.v4.Job.posting_publish_time]
  ///   in days.
  ///   Must specify list of numeric buckets in spec.
  /// * publish_time_in_month: histogram by the
  /// [Job.posting_publish_time][google.cloud.talent.v4.Job.posting_publish_time]
  ///   in months.
  ///   Must specify list of numeric buckets in spec.
  /// * publish_time_in_year: histogram by the
  /// [Job.posting_publish_time][google.cloud.talent.v4.Job.posting_publish_time]
  ///   in years.
  ///   Must specify list of numeric buckets in spec.
  /// * degree_types: histogram by the
  /// [Job.degree_types][google.cloud.talent.v4.Job.degree_types], for example,
  ///   "Bachelors", "Masters".
  /// * job_level: histogram by the
  /// [Job.job_level][google.cloud.talent.v4.Job.job_level], for example, "Entry
  ///   Level".
  /// * country: histogram by the country code of jobs, for example, "US", "FR".
  /// * admin1: histogram by the admin1 code of jobs, which is a global
  ///   placeholder referring to the state, province, or the particular term a
  ///   country uses to define the geographic structure below the country level,
  ///   for example, "CA", "IL".
  /// * city: histogram by a combination of the "city name, admin1 code". For
  ///   example,  "Mountain View, CA", "New York, NY".
  /// * admin1_country: histogram by a combination of the "admin1 code, country",
  ///   for example, "CA, US", "IL, US".
  /// * city_coordinate: histogram by the city center's GPS coordinates (latitude
  ///   and longitude), for example, 37.4038522,-122.0987765. Since the
  ///   coordinates of a city center can change, customers may need to refresh
  ///   them periodically.
  /// * locale: histogram by the
  /// [Job.language_code][google.cloud.talent.v4.Job.language_code], for example,
  /// "en-US",
  ///   "fr-FR".
  /// * language: histogram by the language subtag of the
  /// [Job.language_code][google.cloud.talent.v4.Job.language_code],
  ///   for example, "en", "fr".
  /// * category: histogram by the
  /// [JobCategory][google.cloud.talent.v4.JobCategory], for example,
  ///   "COMPUTER_AND_IT", "HEALTHCARE".
  /// * base_compensation_unit: histogram by the
  ///   [CompensationInfo.CompensationUnit][google.cloud.talent.v4.CompensationInfo.CompensationUnit]
  ///   of base salary, for example, "WEEKLY", "MONTHLY".
  /// * base_compensation: histogram by the base salary. Must specify list of
  ///   numeric buckets to group results by.
  /// * annualized_base_compensation: histogram by the base annualized salary.
  ///   Must specify list of numeric buckets to group results by.
  /// * annualized_total_compensation: histogram by the total annualized salary.
  ///   Must specify list of numeric buckets to group results by.
  /// * string_custom_attribute: histogram by string
  /// [Job.custom_attributes][google.cloud.talent.v4.Job.custom_attributes].
  ///   Values can be accessed via square bracket notations like
  ///   string_custom_attribute["key1"].
  /// * numeric_custom_attribute: histogram by numeric
  /// [Job.custom_attributes][google.cloud.talent.v4.Job.custom_attributes].
  ///   Values can be accessed via square bracket notations like
  ///   numeric_custom_attribute["key1"]. Must specify list of numeric buckets to
  ///   group results by.
  ///
  /// Example expressions:
  ///
  /// * `count(admin1)`
  /// * `count(base_compensation, [bucket(1000, 10000), bucket(10000, 100000),
  /// bucket(100000, MAX)])`
  /// * `count(string_custom_attribute["some-string-custom-attribute"])`
  /// * `count(numeric_custom_attribute["some-numeric-custom-attribute"],
  ///   [bucket(MIN, 0, "negative"), bucket(0, MAX, "non-negative")])`
  ///
  /// [google.cloud.talent.v4.CompanySize]: <doc:CompanySize>
  /// [google.cloud.talent.v4.CompensationInfo.CompensationUnit]: <doc:CompensationInfo/CompensationUnit>
  /// [google.cloud.talent.v4.Job.company_display_name]: <doc:Job/companyDisplayName>
  /// [google.cloud.talent.v4.Job.custom_attributes]: <doc:Job/customAttributes>
  /// [google.cloud.talent.v4.Job.degree_types]: <doc:Job/degreeTypes>
  /// [google.cloud.talent.v4.Job.employment_types]: <doc:Job/employmentTypes>
  /// [google.cloud.talent.v4.Job.job_level]: <doc:Job/jobLevel>
  /// [google.cloud.talent.v4.Job.language_code]: <doc:Job/languageCode>
  /// [google.cloud.talent.v4.Job.posting_publish_time]: <doc:Job/postingPublishTime>
  /// [google.cloud.talent.v4.JobCategory]: <doc:JobCategory>
  public var histogramQueries: [HistogramQuery] = []

  /// The desired job attributes returned for jobs in the search response.
  /// Defaults to
  /// [JobView.JOB_VIEW_SMALL][google.cloud.talent.v4.JobView.JOB_VIEW_SMALL] if
  /// no value is specified.
  ///
  /// [google.cloud.talent.v4.JobView.JOB_VIEW_SMALL]: <doc:JobView/small>
  public var jobView: JobView = JobView()

  /// An integer that specifies the current offset (that is, starting result
  /// location, amongst the jobs deemed by the API as relevant) in search
  /// results. This field is only considered if
  /// [page_token][google.cloud.talent.v4.SearchJobsRequest.page_token] is unset.
  ///
  /// The maximum allowed value is 5000. Otherwise an error is thrown.
  ///
  /// For example, 0 means to  return results starting from the first matching
  /// job, and 10 means to return from the 11th job. This can be used for
  /// pagination, (for example, pageSize = 10 and offset = 10 means to return
  /// from the second page).
  ///
  /// [google.cloud.talent.v4.SearchJobsRequest.page_token]: <doc:SearchJobsRequest/pageToken>
  public var offset: Swift.Int32 = Swift.Int32()

  /// A limit on the number of jobs returned in the search results.
  /// Increasing this value above the default value of 10 can increase search
  /// response time. The value can be between 1 and 100.
  public var maxPageSize: Swift.Int32 = Swift.Int32()

  /// The token specifying the current offset within
  /// search results. See
  /// [SearchJobsResponse.next_page_token][google.cloud.talent.v4.SearchJobsResponse.next_page_token]
  /// for an explanation of how to obtain the next set of query results.
  ///
  /// [google.cloud.talent.v4.SearchJobsResponse.next_page_token]: <doc:SearchJobsResponse/nextPageToken>
  public var pageToken: Swift.String = Swift.String()

  /// The criteria determining how search results are sorted. Default is
  /// `"relevance desc"`.
  ///
  /// Supported options are:
  ///
  /// * `"relevance desc"`: By relevance descending, as determined by the API
  ///   algorithms. Relevance thresholding of query results is only available
  ///   with this ordering.
  /// * `"posting_publish_time desc"`: By
  /// [Job.posting_publish_time][google.cloud.talent.v4.Job.posting_publish_time]
  ///   descending.
  /// * `"posting_update_time desc"`: By
  /// [Job.posting_update_time][google.cloud.talent.v4.Job.posting_update_time]
  ///   descending.
  /// * `"title"`: By [Job.title][google.cloud.talent.v4.Job.title] ascending.
  /// * `"title desc"`: By [Job.title][google.cloud.talent.v4.Job.title]
  /// descending.
  /// * `"annualized_base_compensation"`: By job's
  ///   [CompensationInfo.annualized_base_compensation_range][google.cloud.talent.v4.CompensationInfo.annualized_base_compensation_range]
  ///   ascending. Jobs whose annualized base compensation is unspecified are put
  ///   at the end of search results.
  /// * `"annualized_base_compensation desc"`: By job's
  ///   [CompensationInfo.annualized_base_compensation_range][google.cloud.talent.v4.CompensationInfo.annualized_base_compensation_range]
  ///   descending. Jobs whose annualized base compensation is unspecified are
  ///   put at the end of search results.
  /// * `"annualized_total_compensation"`: By job's
  ///   [CompensationInfo.annualized_total_compensation_range][google.cloud.talent.v4.CompensationInfo.annualized_total_compensation_range]
  ///   ascending. Jobs whose annualized base compensation is unspecified are put
  ///   at the end of search results.
  /// * `"annualized_total_compensation desc"`: By job's
  ///   [CompensationInfo.annualized_total_compensation_range][google.cloud.talent.v4.CompensationInfo.annualized_total_compensation_range]
  ///   descending. Jobs whose annualized base compensation is unspecified are
  ///   put at the end of search results.
  /// * `"custom_ranking desc"`: By the relevance score adjusted to the
  ///   [SearchJobsRequest.CustomRankingInfo.ranking_expression][google.cloud.talent.v4.SearchJobsRequest.CustomRankingInfo.ranking_expression]
  ///   with weight factor assigned by
  ///   [SearchJobsRequest.CustomRankingInfo.importance_level][google.cloud.talent.v4.SearchJobsRequest.CustomRankingInfo.importance_level]
  ///   in descending order.
  /// * Location sorting: Use the special syntax to order jobs by distance:<br>
  ///   `"distance_from('Hawaii')"`: Order by distance from Hawaii.<br>
  ///   `"distance_from(19.89, 155.5)"`: Order by distance from a coordinate.<br>
  ///   `"distance_from('Hawaii'), distance_from('Puerto Rico')"`: Order by
  ///   multiple locations. See details below.<br>
  ///   `"distance_from('Hawaii'), distance_from(19.89, 155.5)"`: Order by
  ///   multiple locations. See details below.<br>
  ///   The string can have a maximum of 256 characters. When multiple distance
  ///   centers are provided, a job that is close to any of the distance centers
  ///   would have a high rank. When a job has multiple locations, the job
  ///   location closest to one of the distance centers will be used. Jobs that
  ///   don't have locations will be ranked at the bottom. Distance is calculated
  ///   with a precision of 11.3 meters (37.4 feet). Diversification strategy is
  ///   still applied unless explicitly disabled in
  ///   [diversification_level][google.cloud.talent.v4.SearchJobsRequest.diversification_level].
  ///
  /// [google.cloud.talent.v4.CompensationInfo.annualized_base_compensation_range]: <doc:CompensationInfo/annualizedBaseCompensationRange>
  /// [google.cloud.talent.v4.CompensationInfo.annualized_total_compensation_range]: <doc:CompensationInfo/annualizedTotalCompensationRange>
  /// [google.cloud.talent.v4.Job.posting_publish_time]: <doc:Job/postingPublishTime>
  /// [google.cloud.talent.v4.Job.posting_update_time]: <doc:Job/postingUpdateTime>
  /// [google.cloud.talent.v4.Job.title]: <doc:Job/title>
  /// [google.cloud.talent.v4.SearchJobsRequest.CustomRankingInfo.importance_level]: <doc:SearchJobsRequest/CustomRankingInfo/importanceLevel>
  /// [google.cloud.talent.v4.SearchJobsRequest.CustomRankingInfo.ranking_expression]: <doc:SearchJobsRequest/CustomRankingInfo/rankingExpression>
  /// [google.cloud.talent.v4.SearchJobsRequest.diversification_level]: <doc:SearchJobsRequest/diversificationLevel>
  public var orderBy: Swift.String = Swift.String()

  /// Controls whether highly similar jobs are returned next to each other in
  /// the search results. Jobs are identified as highly similar based on
  /// their titles, job categories, and locations. Highly similar results are
  /// clustered so that only one representative job of the cluster is
  /// displayed to the job seeker higher up in the results, with the other jobs
  /// being displayed lower down in the results.
  ///
  /// Defaults to
  /// [DiversificationLevel.SIMPLE][google.cloud.talent.v4.SearchJobsRequest.DiversificationLevel.SIMPLE]
  /// if no value is specified.
  ///
  /// [google.cloud.talent.v4.SearchJobsRequest.DiversificationLevel.SIMPLE]: <doc:SearchJobsRequest/DiversificationLevel/simple>
  public var diversificationLevel: SearchJobsRequest.DiversificationLevel =
    SearchJobsRequest.DiversificationLevel()

  /// Controls over how job documents get ranked on top of existing relevance
  /// score (determined by API algorithm).
  public var customRankingInfo: SearchJobsRequest.CustomRankingInfo? = nil

  /// This field is deprecated. Please use
  /// [SearchJobsRequest.keyword_match_mode][google.cloud.talent.v4.SearchJobsRequest.keyword_match_mode]
  /// going forward.
  ///
  /// To migrate, disable_keyword_match set to false maps to
  /// [KeywordMatchMode.KEYWORD_MATCH_ALL][google.cloud.talent.v4.SearchJobsRequest.KeywordMatchMode.KEYWORD_MATCH_ALL],
  /// and disable_keyword_match set to true maps to
  /// [KeywordMatchMode.KEYWORD_MATCH_DISABLED][google.cloud.talent.v4.SearchJobsRequest.KeywordMatchMode.KEYWORD_MATCH_DISABLED].
  /// If
  /// [SearchJobsRequest.keyword_match_mode][google.cloud.talent.v4.SearchJobsRequest.keyword_match_mode]
  /// is set, this field is ignored.
  ///
  /// Controls whether to disable exact keyword match on
  /// [Job.title][google.cloud.talent.v4.Job.title],
  /// [Job.description][google.cloud.talent.v4.Job.description],
  /// [Job.company_display_name][google.cloud.talent.v4.Job.company_display_name],
  /// [Job.addresses][google.cloud.talent.v4.Job.addresses],
  /// [Job.qualifications][google.cloud.talent.v4.Job.qualifications]. When
  /// disable keyword match is turned off, a keyword match returns jobs that do
  /// not match given category filters when there are matching keywords. For
  /// example, for the query "program manager," a result is returned even if the
  /// job posting has the title "software developer," which doesn't fall into
  /// "program manager" ontology, but does have "program manager" appearing in
  /// its description.
  ///
  /// For queries like "cloud" that don't contain title or
  /// location specific ontology, jobs with "cloud" keyword matches are returned
  /// regardless of this flag's value.
  ///
  /// Use
  /// [Company.keyword_searchable_job_custom_attributes][google.cloud.talent.v4.Company.keyword_searchable_job_custom_attributes]
  /// if company-specific globally matched custom field/attribute string values
  /// are needed. Enabling keyword match improves recall of subsequent search
  /// requests.
  ///
  /// Defaults to false.
  ///
  /// [google.cloud.talent.v4.Company.keyword_searchable_job_custom_attributes]: <doc:Company/keywordSearchableJobCustomAttributes>
  /// [google.cloud.talent.v4.Job.addresses]: <doc:Job/addresses>
  /// [google.cloud.talent.v4.Job.company_display_name]: <doc:Job/companyDisplayName>
  /// [google.cloud.talent.v4.Job.description]: <doc:Job/description>
  /// [google.cloud.talent.v4.Job.qualifications]: <doc:Job/qualifications>
  /// [google.cloud.talent.v4.Job.title]: <doc:Job/title>
  /// [google.cloud.talent.v4.SearchJobsRequest.KeywordMatchMode.KEYWORD_MATCH_ALL]: <doc:SearchJobsRequest/KeywordMatchMode/keywordMatchAll>
  /// [google.cloud.talent.v4.SearchJobsRequest.KeywordMatchMode.KEYWORD_MATCH_DISABLED]: <doc:SearchJobsRequest/KeywordMatchMode/keywordMatchDisabled>
  /// [google.cloud.talent.v4.SearchJobsRequest.keyword_match_mode]: <doc:SearchJobsRequest/keywordMatchMode>
  @available(*, deprecated)
  public var disableKeywordMatch: Swift.Bool = Swift.Bool()

  /// Controls what keyword match options to use. If both keyword_match_mode and
  /// disable_keyword_match are set, keyword_match_mode will take precedence.
  ///
  /// Defaults to
  /// [KeywordMatchMode.KEYWORD_MATCH_ALL][google.cloud.talent.v4.SearchJobsRequest.KeywordMatchMode.KEYWORD_MATCH_ALL]
  /// if no value is specified.
  ///
  /// [google.cloud.talent.v4.SearchJobsRequest.KeywordMatchMode.KEYWORD_MATCH_ALL]: <doc:SearchJobsRequest/KeywordMatchMode/keywordMatchAll>
  public var keywordMatchMode: SearchJobsRequest.KeywordMatchMode =
    SearchJobsRequest.KeywordMatchMode()

  /// Optional. The relevance threshold of the search results.
  ///
  /// Default to Google defined threshold, leveraging a balance of
  /// precision and recall to deliver both highly accurate results and
  /// comprehensive coverage of relevant information.
  public var relevanceThreshold: SearchJobsRequest.RelevanceThreshold =
    SearchJobsRequest.RelevanceThreshold()

  /// Initialize a new instance of `SearchJobsRequest`.
  public init() {}

  /// Use `config` to return a new instance of this object, with some fields updated.
  ///
  /// Commonly used to initialize the value, for example:
  ///
  /// ```
  /// let value = SearchJobsRequest().with { $0.parent = ... }
  /// ```
  public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
    var copy = self
    try config(&copy)
    return copy
  }

  /// Custom ranking information for
  /// [SearchJobsRequest][google.cloud.talent.v4.SearchJobsRequest].
  ///
  /// [google.cloud.talent.v4.SearchJobsRequest]: <doc:SearchJobsRequest>
  public struct CustomRankingInfo: Codable, Equatable, GoogleCloudWkt._AnyPackable,
    Sendable
  {
    /// Required. Controls over how important the score of
    /// [CustomRankingInfo.ranking_expression][google.cloud.talent.v4.SearchJobsRequest.CustomRankingInfo.ranking_expression]
    /// gets applied to job's final ranking position.
    ///
    /// An error is thrown if not specified.
    ///
    /// [google.cloud.talent.v4.SearchJobsRequest.CustomRankingInfo.ranking_expression]: <doc:SearchJobsRequest/CustomRankingInfo/rankingExpression>
    public var importanceLevel: SearchJobsRequest.CustomRankingInfo.ImportanceLevel =
      SearchJobsRequest.CustomRankingInfo.ImportanceLevel()

    /// Required. Controls over how job documents get ranked on top of existing
    /// relevance score (determined by API algorithm). A combination of the
    /// ranking expression and relevance score is used to determine job's final
    /// ranking position.
    ///
    /// The syntax for this expression is a subset of Google SQL syntax.
    ///
    /// Supported operators are: +, -, *, /, where the left and right side of
    /// the operator is either a numeric
    /// [Job.custom_attributes][google.cloud.talent.v4.Job.custom_attributes]
    /// key, integer/double value or an expression that can be evaluated to a
    /// number.
    ///
    /// Parenthesis are supported to adjust calculation precedence. The
    /// expression must be < 200 characters in length.
    ///
    /// The expression is considered invalid for a job if the expression
    /// references custom attributes that are not populated on the job or if the
    /// expression results in a divide by zero. If an expression is invalid for a
    /// job, that job is demoted to the end of the results.
    ///
    /// Sample ranking expression
    /// (year + 25) * 0.25 - (freshness / 0.5)
    ///
    /// [google.cloud.talent.v4.Job.custom_attributes]: <doc:Job/customAttributes>
    public var rankingExpression: Swift.String = Swift.String()

    /// Initialize a new instance of `CustomRankingInfo`.
    public init() {}

    /// Use `config` to return a new instance of this object, with some fields updated.
    ///
    /// Commonly used to initialize the value, for example:
    ///
    /// ```
    /// let value = CustomRankingInfo().with { $0.importanceLevel = ... }
    /// ```
    public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
      var copy = self
      try config(&copy)
      return copy
    }

    /// The importance level for
    /// [CustomRankingInfo.ranking_expression][google.cloud.talent.v4.SearchJobsRequest.CustomRankingInfo.ranking_expression].
    ///
    /// [google.cloud.talent.v4.SearchJobsRequest.CustomRankingInfo.ranking_expression]: <doc:SearchJobsRequest/CustomRankingInfo/rankingExpression>
    public enum ImportanceLevel: Codable, Equatable, Sendable {
      /// Default value if the importance level isn't specified.
      case unspecified
      /// The given ranking expression is of None importance, existing relevance
      /// score (determined by API algorithm) dominates job's final ranking
      /// position.
      case `none`
      /// The given ranking expression is of Low importance in terms of job's
      /// final ranking position compared to existing relevance
      /// score (determined by API algorithm).
      case low
      /// The given ranking expression is of Mild importance in terms of job's
      /// final ranking position compared to existing relevance
      /// score (determined by API algorithm).
      case mild
      /// The given ranking expression is of Medium importance in terms of job's
      /// final ranking position compared to existing relevance
      /// score (determined by API algorithm).
      case medium
      /// The given ranking expression is of High importance in terms of job's
      /// final ranking position compared to existing relevance
      /// score (determined by API algorithm).
      case high
      /// The given ranking expression is of Extreme importance, and dominates
      /// job's final ranking position with existing relevance
      /// score (determined by API algorithm) ignored.
      case extreme
      /// Encodes an unknown integer value.
      ///
      /// The most common cause for an unknown values is for the service to send
      /// a value unknown to the library. We recommend you update your library to
      /// the latest version.
      case unknownIntValue(Int)
      /// Encodes an unknown string value.
      ///
      /// The most common cause for an unknown values is for the service to send
      /// a value unknown to the library. We recommend you update your library to
      /// the latest version.
      case unknownStringValue(String)

      public init() {
        self = .unspecified
      }

      /// Returns the integer value associated with the enumeration.
      ///
      /// If the enumeration was initialized with an unknown string value, this returns `nil`.
      public var intValue: Int? {
        switch self {
        case .unspecified: return 0
        case .`none`: return 1
        case .low: return 2
        case .mild: return 3
        case .medium: return 4
        case .high: return 5
        case .extreme: return 6
        case .unknownIntValue(let v): return v
        case .unknownStringValue: return nil
        }
      }

      /// Returns the string value (or name) associated with the enumeration.
      ///
      /// If the enumeration was initialized with an unknown integer value, this returns `nil`.
      public var stringValue: Swift.String? {
        switch self {
        case .unspecified: return "IMPORTANCE_LEVEL_UNSPECIFIED"
        case .`none`: return "NONE"
        case .low: return "LOW"
        case .mild: return "MILD"
        case .medium: return "MEDIUM"
        case .high: return "HIGH"
        case .extreme: return "EXTREME"
        case .unknownIntValue: return nil
        case .unknownStringValue(let v): return v
        }
      }

      /// Initialize from a string value.
      ///
      /// If the value is unknown, this initializes to [`unknownStringValue`](doc:ImportanceLevel/unknownStringValue(_:)).
      public init(stringValue: Swift.String) {
        switch stringValue {
        case "IMPORTANCE_LEVEL_UNSPECIFIED": self = .unspecified
        case "NONE": self = .`none`
        case "LOW": self = .low
        case "MILD": self = .mild
        case "MEDIUM": self = .medium
        case "HIGH": self = .high
        case "EXTREME": self = .extreme
        default: self = .unknownStringValue(stringValue)
        }
      }

      /// Initialize from an integer value.
      ///
      /// If the value is unknown, this initializes to [`unknownIntValue`](doc:ImportanceLevel/unknownIntValue(_:)).
      public init(intValue: Int) {
        switch intValue {
        case 0: self = .unspecified
        case 1: self = .`none`
        case 2: self = .low
        case 3: self = .mild
        case 4: self = .medium
        case 5: self = .high
        case 6: self = .extreme
        default: self = .unknownIntValue(intValue)
        }
      }

      public init(from decoder: Decoder) throws {
        let container = try decoder.singleValueContainer()
        if let v = try? container.decode(Int.self) {
          self.init(intValue: v)
          return
        }
        if let s = try? container.decode(String.self) {
          if let v = Int(s) {
            self.init(intValue: v)
          } else {
            self.init(stringValue: s)
          }
          return
        }
        throw DecodingError.dataCorruptedError(
          in: container, debugDescription: "Expected enum value, must be integer or string.")
      }

      public func encode(to encoder: Encoder) throws {
        var container = encoder.singleValueContainer()
        switch self {
        case .unspecified: return try container.encode(0)
        case .`none`: return try container.encode(1)
        case .low: return try container.encode(2)
        case .mild: return try container.encode(3)
        case .medium: return try container.encode(4)
        case .high: return try container.encode(5)
        case .extreme: return try container.encode(6)
        case .unknownIntValue(let v): return try container.encode(v)
        case .unknownStringValue(let v): return try container.encode(v)
        }
      }
    }

    public static var _anyTypeUrl: Swift.String {
      return "type.googleapis.com/google.cloud.talent.v4.SearchJobsRequest.CustomRankingInfo"
    }
    public init(fromAny any: GoogleCloudWkt.`Any`) throws {
      self = try GoogleCloudWkt._slowAnyDeserialize(Self.self, from: any)
    }
    public func _pack() throws -> GoogleCloudWkt.Struct {
      return try GoogleCloudWkt._slowAnySerialize(message: self)
    }
  }

  /// A string-represented enumeration of the job search mode. The service
  /// operate differently for different modes of service.
  public enum SearchMode: Codable, Equatable, Sendable {
    /// The mode of the search method isn't specified. The default search
    /// behavior is identical to JOB_SEARCH search behavior.
    case unspecified
    /// The job search matches against all jobs, and featured jobs
    /// (jobs with promotionValue > 0) are not specially handled.
    case jobSearch
    /// The job search matches only against featured jobs (jobs with a
    /// promotionValue > 0). This method doesn't return any jobs having a
    /// promotionValue <= 0. The search results order is determined by the
    /// promotionValue (jobs with a higher promotionValue are returned higher up
    /// in the search results), with relevance being used as a tiebreaker.
    case featuredJobSearch
    /// Encodes an unknown integer value.
    ///
    /// The most common cause for an unknown values is for the service to send
    /// a value unknown to the library. We recommend you update your library to
    /// the latest version.
    case unknownIntValue(Int)
    /// Encodes an unknown string value.
    ///
    /// The most common cause for an unknown values is for the service to send
    /// a value unknown to the library. We recommend you update your library to
    /// the latest version.
    case unknownStringValue(String)

    public init() {
      self = .unspecified
    }

    /// Returns the integer value associated with the enumeration.
    ///
    /// If the enumeration was initialized with an unknown string value, this returns `nil`.
    public var intValue: Int? {
      switch self {
      case .unspecified: return 0
      case .jobSearch: return 1
      case .featuredJobSearch: return 2
      case .unknownIntValue(let v): return v
      case .unknownStringValue: return nil
      }
    }

    /// Returns the string value (or name) associated with the enumeration.
    ///
    /// If the enumeration was initialized with an unknown integer value, this returns `nil`.
    public var stringValue: Swift.String? {
      switch self {
      case .unspecified: return "SEARCH_MODE_UNSPECIFIED"
      case .jobSearch: return "JOB_SEARCH"
      case .featuredJobSearch: return "FEATURED_JOB_SEARCH"
      case .unknownIntValue: return nil
      case .unknownStringValue(let v): return v
      }
    }

    /// Initialize from a string value.
    ///
    /// If the value is unknown, this initializes to [`unknownStringValue`](doc:SearchMode/unknownStringValue(_:)).
    public init(stringValue: Swift.String) {
      switch stringValue {
      case "SEARCH_MODE_UNSPECIFIED": self = .unspecified
      case "JOB_SEARCH": self = .jobSearch
      case "FEATURED_JOB_SEARCH": self = .featuredJobSearch
      default: self = .unknownStringValue(stringValue)
      }
    }

    /// Initialize from an integer value.
    ///
    /// If the value is unknown, this initializes to [`unknownIntValue`](doc:SearchMode/unknownIntValue(_:)).
    public init(intValue: Int) {
      switch intValue {
      case 0: self = .unspecified
      case 1: self = .jobSearch
      case 2: self = .featuredJobSearch
      default: self = .unknownIntValue(intValue)
      }
    }

    public init(from decoder: Decoder) throws {
      let container = try decoder.singleValueContainer()
      if let v = try? container.decode(Int.self) {
        self.init(intValue: v)
        return
      }
      if let s = try? container.decode(String.self) {
        if let v = Int(s) {
          self.init(intValue: v)
        } else {
          self.init(stringValue: s)
        }
        return
      }
      throw DecodingError.dataCorruptedError(
        in: container, debugDescription: "Expected enum value, must be integer or string.")
    }

    public func encode(to encoder: Encoder) throws {
      var container = encoder.singleValueContainer()
      switch self {
      case .unspecified: return try container.encode(0)
      case .jobSearch: return try container.encode(1)
      case .featuredJobSearch: return try container.encode(2)
      case .unknownIntValue(let v): return try container.encode(v)
      case .unknownStringValue(let v): return try container.encode(v)
      }
    }
  }

  /// Controls whether highly similar jobs are returned next to each other in
  /// the search results. Jobs are identified as highly similar based on
  /// their titles, job categories, and locations. Highly similar results are
  /// clustered so that only one representative job of the cluster is
  /// displayed to the job seeker higher up in the results, with the other jobs
  /// being displayed lower down in the results.
  ///
  /// If you are using pageToken to page through the result set,
  /// latency might be lower but we can't guarantee that all results are
  /// returned. If you are using page offset, latency might be higher but all
  /// results are returned.
  public enum DiversificationLevel: Codable, Equatable, Sendable {
    /// The diversification level isn't specified.
    case unspecified
    /// Disables diversification. Jobs that would normally be pushed to the last
    /// page would not have their positions altered. This may result in highly
    /// similar jobs appearing in sequence in the search results.
    case disabled
    /// Default diversifying behavior. The result list is ordered so that
    /// highly similar results are pushed to the end of the last page of search
    /// results.
    case simple
    /// Only one job from the same company will be shown at once, other jobs
    /// under same company are pushed to the end of the last page of search
    /// result.
    case onePerCompany
    /// Similar to ONE_PER_COMPANY, but it allows at most two jobs in the
    /// same company to be shown at once, the other jobs under same company are
    /// pushed to the end of the last page of search result.
    case twoPerCompany
    /// The result list is ordered such that somewhat similar results are pushed
    /// to the end of the last page of the search results. This option is
    /// recommended if SIMPLE diversification does not diversify enough.
    case diversifyByLooserSimilarity
    /// Similar to ONE_PER_COMPANY, but it allows at most three jobs in the
    /// same company to be shown at once, the other jobs under same company are
    /// dropped.
    case maxThreePerCompany
    /// Encodes an unknown integer value.
    ///
    /// The most common cause for an unknown values is for the service to send
    /// a value unknown to the library. We recommend you update your library to
    /// the latest version.
    case unknownIntValue(Int)
    /// Encodes an unknown string value.
    ///
    /// The most common cause for an unknown values is for the service to send
    /// a value unknown to the library. We recommend you update your library to
    /// the latest version.
    case unknownStringValue(String)

    public init() {
      self = .unspecified
    }

    /// Returns the integer value associated with the enumeration.
    ///
    /// If the enumeration was initialized with an unknown string value, this returns `nil`.
    public var intValue: Int? {
      switch self {
      case .unspecified: return 0
      case .disabled: return 1
      case .simple: return 2
      case .onePerCompany: return 3
      case .twoPerCompany: return 4
      case .diversifyByLooserSimilarity: return 5
      case .maxThreePerCompany: return 6
      case .unknownIntValue(let v): return v
      case .unknownStringValue: return nil
      }
    }

    /// Returns the string value (or name) associated with the enumeration.
    ///
    /// If the enumeration was initialized with an unknown integer value, this returns `nil`.
    public var stringValue: Swift.String? {
      switch self {
      case .unspecified: return "DIVERSIFICATION_LEVEL_UNSPECIFIED"
      case .disabled: return "DISABLED"
      case .simple: return "SIMPLE"
      case .onePerCompany: return "ONE_PER_COMPANY"
      case .twoPerCompany: return "TWO_PER_COMPANY"
      case .diversifyByLooserSimilarity: return "DIVERSIFY_BY_LOOSER_SIMILARITY"
      case .maxThreePerCompany: return "MAX_THREE_PER_COMPANY"
      case .unknownIntValue: return nil
      case .unknownStringValue(let v): return v
      }
    }

    /// Initialize from a string value.
    ///
    /// If the value is unknown, this initializes to [`unknownStringValue`](doc:DiversificationLevel/unknownStringValue(_:)).
    public init(stringValue: Swift.String) {
      switch stringValue {
      case "DIVERSIFICATION_LEVEL_UNSPECIFIED": self = .unspecified
      case "DISABLED": self = .disabled
      case "SIMPLE": self = .simple
      case "ONE_PER_COMPANY": self = .onePerCompany
      case "TWO_PER_COMPANY": self = .twoPerCompany
      case "MAX_THREE_PER_COMPANY": self = .maxThreePerCompany
      case "DIVERSIFY_BY_LOOSER_SIMILARITY": self = .diversifyByLooserSimilarity
      default: self = .unknownStringValue(stringValue)
      }
    }

    /// Initialize from an integer value.
    ///
    /// If the value is unknown, this initializes to [`unknownIntValue`](doc:DiversificationLevel/unknownIntValue(_:)).
    public init(intValue: Int) {
      switch intValue {
      case 0: self = .unspecified
      case 1: self = .disabled
      case 2: self = .simple
      case 3: self = .onePerCompany
      case 4: self = .twoPerCompany
      case 5: self = .diversifyByLooserSimilarity
      case 6: self = .maxThreePerCompany
      default: self = .unknownIntValue(intValue)
      }
    }

    public init(from decoder: Decoder) throws {
      let container = try decoder.singleValueContainer()
      if let v = try? container.decode(Int.self) {
        self.init(intValue: v)
        return
      }
      if let s = try? container.decode(String.self) {
        if let v = Int(s) {
          self.init(intValue: v)
        } else {
          self.init(stringValue: s)
        }
        return
      }
      throw DecodingError.dataCorruptedError(
        in: container, debugDescription: "Expected enum value, must be integer or string.")
    }

    public func encode(to encoder: Encoder) throws {
      var container = encoder.singleValueContainer()
      switch self {
      case .unspecified: return try container.encode(0)
      case .disabled: return try container.encode(1)
      case .simple: return try container.encode(2)
      case .onePerCompany: return try container.encode(3)
      case .twoPerCompany: return try container.encode(4)
      case .diversifyByLooserSimilarity: return try container.encode(5)
      case .maxThreePerCompany: return try container.encode(6)
      case .unknownIntValue(let v): return try container.encode(v)
      case .unknownStringValue(let v): return try container.encode(v)
      }
    }
  }

  /// Controls what keyword matching behavior the search has. When keyword
  /// matching is enabled, a keyword match returns jobs that may not match given
  /// category filters when there are matching keywords. For example, for the
  /// query "program manager" with KeywordMatchMode set to KEYWORD_MATCH_ALL, a
  /// job posting with the title "software developer," which doesn't fall into
  /// "program manager" ontology, and "program manager" appearing in its
  /// description will be surfaced.
  ///
  /// For queries like "cloud" that don't contain title or
  /// location specific ontology, jobs with "cloud" keyword matches are returned
  /// regardless of this enum's value.
  ///
  /// Use
  /// [Company.keyword_searchable_job_custom_attributes][google.cloud.talent.v4.Company.keyword_searchable_job_custom_attributes]
  /// if company-specific globally matched custom field/attribute string values
  /// are needed. Enabling keyword match improves recall of subsequent search
  /// requests.
  ///
  /// [google.cloud.talent.v4.Company.keyword_searchable_job_custom_attributes]: <doc:Company/keywordSearchableJobCustomAttributes>
  public enum KeywordMatchMode: Codable, Equatable, Sendable {
    /// The keyword match option isn't specified. Defaults to
    /// [KeywordMatchMode.KEYWORD_MATCH_ALL][google.cloud.talent.v4.SearchJobsRequest.KeywordMatchMode.KEYWORD_MATCH_ALL]
    /// behavior.
    ///
    /// [google.cloud.talent.v4.SearchJobsRequest.KeywordMatchMode.KEYWORD_MATCH_ALL]: <doc:SearchJobsRequest/KeywordMatchMode/keywordMatchAll>
    case unspecified
    /// Disables keyword matching.
    case keywordMatchDisabled
    /// Enable keyword matching over
    /// [Job.title][google.cloud.talent.v4.Job.title],
    /// [Job.description][google.cloud.talent.v4.Job.description],
    /// [Job.company_display_name][google.cloud.talent.v4.Job.company_display_name],
    /// [Job.addresses][google.cloud.talent.v4.Job.addresses],
    /// [Job.qualifications][google.cloud.talent.v4.Job.qualifications], and
    /// keyword searchable
    /// [Job.custom_attributes][google.cloud.talent.v4.Job.custom_attributes]
    /// fields.
    ///
    /// [google.cloud.talent.v4.Job.addresses]: <doc:Job/addresses>
    /// [google.cloud.talent.v4.Job.company_display_name]: <doc:Job/companyDisplayName>
    /// [google.cloud.talent.v4.Job.custom_attributes]: <doc:Job/customAttributes>
    /// [google.cloud.talent.v4.Job.description]: <doc:Job/description>
    /// [google.cloud.talent.v4.Job.qualifications]: <doc:Job/qualifications>
    /// [google.cloud.talent.v4.Job.title]: <doc:Job/title>
    case keywordMatchAll
    /// Only enable keyword matching over
    /// [Job.title][google.cloud.talent.v4.Job.title].
    ///
    /// [google.cloud.talent.v4.Job.title]: <doc:Job/title>
    case keywordMatchTitleOnly
    /// Encodes an unknown integer value.
    ///
    /// The most common cause for an unknown values is for the service to send
    /// a value unknown to the library. We recommend you update your library to
    /// the latest version.
    case unknownIntValue(Int)
    /// Encodes an unknown string value.
    ///
    /// The most common cause for an unknown values is for the service to send
    /// a value unknown to the library. We recommend you update your library to
    /// the latest version.
    case unknownStringValue(String)

    public init() {
      self = .unspecified
    }

    /// Returns the integer value associated with the enumeration.
    ///
    /// If the enumeration was initialized with an unknown string value, this returns `nil`.
    public var intValue: Int? {
      switch self {
      case .unspecified: return 0
      case .keywordMatchDisabled: return 1
      case .keywordMatchAll: return 2
      case .keywordMatchTitleOnly: return 3
      case .unknownIntValue(let v): return v
      case .unknownStringValue: return nil
      }
    }

    /// Returns the string value (or name) associated with the enumeration.
    ///
    /// If the enumeration was initialized with an unknown integer value, this returns `nil`.
    public var stringValue: Swift.String? {
      switch self {
      case .unspecified: return "KEYWORD_MATCH_MODE_UNSPECIFIED"
      case .keywordMatchDisabled: return "KEYWORD_MATCH_DISABLED"
      case .keywordMatchAll: return "KEYWORD_MATCH_ALL"
      case .keywordMatchTitleOnly: return "KEYWORD_MATCH_TITLE_ONLY"
      case .unknownIntValue: return nil
      case .unknownStringValue(let v): return v
      }
    }

    /// Initialize from a string value.
    ///
    /// If the value is unknown, this initializes to [`unknownStringValue`](doc:KeywordMatchMode/unknownStringValue(_:)).
    public init(stringValue: Swift.String) {
      switch stringValue {
      case "KEYWORD_MATCH_MODE_UNSPECIFIED": self = .unspecified
      case "KEYWORD_MATCH_DISABLED": self = .keywordMatchDisabled
      case "KEYWORD_MATCH_ALL": self = .keywordMatchAll
      case "KEYWORD_MATCH_TITLE_ONLY": self = .keywordMatchTitleOnly
      default: self = .unknownStringValue(stringValue)
      }
    }

    /// Initialize from an integer value.
    ///
    /// If the value is unknown, this initializes to [`unknownIntValue`](doc:KeywordMatchMode/unknownIntValue(_:)).
    public init(intValue: Int) {
      switch intValue {
      case 0: self = .unspecified
      case 1: self = .keywordMatchDisabled
      case 2: self = .keywordMatchAll
      case 3: self = .keywordMatchTitleOnly
      default: self = .unknownIntValue(intValue)
      }
    }

    public init(from decoder: Decoder) throws {
      let container = try decoder.singleValueContainer()
      if let v = try? container.decode(Int.self) {
        self.init(intValue: v)
        return
      }
      if let s = try? container.decode(String.self) {
        if let v = Int(s) {
          self.init(intValue: v)
        } else {
          self.init(stringValue: s)
        }
        return
      }
      throw DecodingError.dataCorruptedError(
        in: container, debugDescription: "Expected enum value, must be integer or string.")
    }

    public func encode(to encoder: Encoder) throws {
      var container = encoder.singleValueContainer()
      switch self {
      case .unspecified: return try container.encode(0)
      case .keywordMatchDisabled: return try container.encode(1)
      case .keywordMatchAll: return try container.encode(2)
      case .keywordMatchTitleOnly: return try container.encode(3)
      case .unknownIntValue(let v): return try container.encode(v)
      case .unknownStringValue(let v): return try container.encode(v)
      }
    }
  }

  /// The relevance threshold of the search results. The higher relevance
  /// threshold is, the higher relevant results are shown and the less number of
  /// results are returned.
  public enum RelevanceThreshold: Codable, Equatable, Sendable {
    /// Default value. In this case, server behavior defaults to Google defined
    /// threshold.
    case unspecified
    /// Lowest relevance threshold.
    case lowest
    /// Low relevance threshold.
    case low
    /// Medium relevance threshold.
    case medium
    /// High relevance threshold.
    case high
    /// Encodes an unknown integer value.
    ///
    /// The most common cause for an unknown values is for the service to send
    /// a value unknown to the library. We recommend you update your library to
    /// the latest version.
    case unknownIntValue(Int)
    /// Encodes an unknown string value.
    ///
    /// The most common cause for an unknown values is for the service to send
    /// a value unknown to the library. We recommend you update your library to
    /// the latest version.
    case unknownStringValue(String)

    public init() {
      self = .unspecified
    }

    /// Returns the integer value associated with the enumeration.
    ///
    /// If the enumeration was initialized with an unknown string value, this returns `nil`.
    public var intValue: Int? {
      switch self {
      case .unspecified: return 0
      case .lowest: return 1
      case .low: return 2
      case .medium: return 3
      case .high: return 4
      case .unknownIntValue(let v): return v
      case .unknownStringValue: return nil
      }
    }

    /// Returns the string value (or name) associated with the enumeration.
    ///
    /// If the enumeration was initialized with an unknown integer value, this returns `nil`.
    public var stringValue: Swift.String? {
      switch self {
      case .unspecified: return "RELEVANCE_THRESHOLD_UNSPECIFIED"
      case .lowest: return "LOWEST"
      case .low: return "LOW"
      case .medium: return "MEDIUM"
      case .high: return "HIGH"
      case .unknownIntValue: return nil
      case .unknownStringValue(let v): return v
      }
    }

    /// Initialize from a string value.
    ///
    /// If the value is unknown, this initializes to [`unknownStringValue`](doc:RelevanceThreshold/unknownStringValue(_:)).
    public init(stringValue: Swift.String) {
      switch stringValue {
      case "RELEVANCE_THRESHOLD_UNSPECIFIED": self = .unspecified
      case "LOWEST": self = .lowest
      case "LOW": self = .low
      case "MEDIUM": self = .medium
      case "HIGH": self = .high
      default: self = .unknownStringValue(stringValue)
      }
    }

    /// Initialize from an integer value.
    ///
    /// If the value is unknown, this initializes to [`unknownIntValue`](doc:RelevanceThreshold/unknownIntValue(_:)).
    public init(intValue: Int) {
      switch intValue {
      case 0: self = .unspecified
      case 1: self = .lowest
      case 2: self = .low
      case 3: self = .medium
      case 4: self = .high
      default: self = .unknownIntValue(intValue)
      }
    }

    public init(from decoder: Decoder) throws {
      let container = try decoder.singleValueContainer()
      if let v = try? container.decode(Int.self) {
        self.init(intValue: v)
        return
      }
      if let s = try? container.decode(String.self) {
        if let v = Int(s) {
          self.init(intValue: v)
        } else {
          self.init(stringValue: s)
        }
        return
      }
      throw DecodingError.dataCorruptedError(
        in: container, debugDescription: "Expected enum value, must be integer or string.")
    }

    public func encode(to encoder: Encoder) throws {
      var container = encoder.singleValueContainer()
      switch self {
      case .unspecified: return try container.encode(0)
      case .lowest: return try container.encode(1)
      case .low: return try container.encode(2)
      case .medium: return try container.encode(3)
      case .high: return try container.encode(4)
      case .unknownIntValue(let v): return try container.encode(v)
      case .unknownStringValue(let v): return try container.encode(v)
      }
    }
  }

  public static var _anyTypeUrl: Swift.String {
    return "type.googleapis.com/google.cloud.talent.v4.SearchJobsRequest"
  }
  public init(fromAny any: GoogleCloudWkt.`Any`) throws {
    self = try GoogleCloudWkt._slowAnyDeserialize(Self.self, from: any)
  }
  public func _pack() throws -> GoogleCloudWkt.Struct {
    return try GoogleCloudWkt._slowAnySerialize(message: self)
  }
}
